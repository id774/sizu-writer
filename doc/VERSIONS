sizu-writer Repository Version History
======================================

v1.0.3 (Release Date: TBD)
--------------------------
- Refuse a non-finite --timeout override, a whitespace-only --model override and
  an unsupported LOG_LEVEL instead of accepting them.
- Ship GENERATION_MODEL blank in .env.example instead of a concrete model name.
- Refuse title-only regeneration without a body and preserve title-only mode
  when retrying a failed generation.
- Preserve memo and body text literally during prompt substitution and refuse
  empty prompt files before generation.
- Require generation responses to report a finish reason, correlate failures by
  error id, and keep raw upstream error text out of logs.
- Preserve blank lines and headings inside backtick and tilde fenced code blocks
  during Markdown normalization.
- Preserve title-only regeneration and its settled body when memo validation
  fails before generation.
- Complete the designed Web character count, submit state, body auto-growth and
  result memo length limit.
- Refuse generation responses that omit the required alternative_titles field.
- Refuse malformed GENERATION_BASE_URL hosts, ports and whitespace at startup.

v1.0.2 (2026-08-26)
-------------------
- Preserve separate fenced code blocks at the boundaries of generated Markdown
  bodies.
- Keep the web buttons legible on a phone in dark appearance, declaring the light
  color scheme and stating the button text color with its background.
- Complete the versioning rules in doc/POLICY.md.
- Refuse non-finite values in numeric configuration instead of passing them to the
  generation client or raising an unexpected conversion error.

v1.0.1 (2026-08-09)
-------------------
- Add .gitattributes, marking the Markdown documents for diff.
- Rename doc/POLICY and doc/LICENSE to doc/POLICY.md and doc/LICENSE.md, so that
  GitHub renders them, and update the references to them.
- Expand doc/POLICY.md into a standalone policy, adding the rules it had deferred
  elsewhere: pull request scope and version history width.
- Add the shared version history description guidelines to the end of this file.

v1.0 (2026-08-05)
-----------------
- Initial release.

Version History Guidelines
==========================
- Write one coherent change per bullet, at most two physical lines.
- A single line at or under 80 columns is preferred whenever practical.
  Necessary file names, command names, option names, and configuration
  names may pass 80 columns on the bullet's one or two lines when they
  cannot be shortened without losing meaning.
- When a version history already exists, balance a new entry against the
  length of the existing lines, so that the whole file stays consistent.
  Holding to the form this file has settled on comes before the two-line,
  80-column limit above, and leaves that limit standing wherever no such
  form exists.
- When an entry becomes substantially longer, omit or abstract implementation
  details, examples, reasons, and secondary effects where possible.
- Preserve the changed target, externally observable behavior, compatibility
  or security impact, and important option or configuration names.
- Combine related changes within the same version, especially changes to the
  same file or feature.
- Place closely related entries near each other.
- Append an independent new change to the end of the current version entry.
- The first entry, at the lowest version this file reaches, reads only
  "Initial release." and nothing else.
